{
  // Forme documentaire. Pour l'appliquer vraiment :
  // node nimbus/cli/nimbus.mjs permissions apply --config ~/.openclaw/openclaw.json
  // validate=false
  agents: {
    defaults: {
      workspace: "~/.openclaw/workspace",
    },
  },
  tools: {
    exec: {
      mode: "deny",
    },
  },
}
